#ifndef _MSET_DEFINED
#define _MSET_DEFINED

#define _MSET_DELETE   1	// remove all selected from 'sedit'
#define _MSET_RIGHTBANK  2	// rightmost column, which is available when editing GET (for scrolling in GetSys )
#define _MSET_PRINTNUM 3	// number of the printer, which is printed
#define _MSET_PRINTNAME 4	// name of the printer, which is printed
#define _MSET_PROGRESS 5	// do not show runner

#endif
